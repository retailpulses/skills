interface:
  display_name: "Mercari Category ID"
  short_description: "Write Mercari category IDs into Supabase product_variants from live master data."
  default_prompt: "Use $mercari-category-id to inspect the live Supabase schema or CSV header, match Product Name against the category master, run a 100-row pilot if needed, and write Mercari category IDs back to product_variants or into an output CSV."
